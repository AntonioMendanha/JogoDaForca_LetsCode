import { CorrectLetterDirective } from './correct-letter.directive';

describe('CorrectLetterDirective', () => {
  it('should create an instance', () => {
    const directive = new CorrectLetterDirective();
    expect(directive).toBeTruthy();
  });
});
